# from os.path import dirname, isfile, join
# import glob
# import re

# modules = glob.glob(join(dirname(__file__), "*/*.py"))
# __all__ = [ (re.search('\/(\w+)?\/model.py$', f).group(1) + ".model") for f in modules if isfile(f) and f.endswith("model.py") ]
